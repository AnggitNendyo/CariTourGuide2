<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="d3ti.uns.anggit.caritourguide">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EditServiceTourguideActivity"></activity>
        <activity android:name=".EditProfilWisatawanActivity" />
        <activity android:name=".EditProfilTourguideActivity" />
        <activity android:name=".DetailTourguideActivity" />
        <activity android:name=".DetailPemesananTourguideActivity" />
        <activity android:name=".DetailPemesananWisatawanActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".MainActivityTourguide" />
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>